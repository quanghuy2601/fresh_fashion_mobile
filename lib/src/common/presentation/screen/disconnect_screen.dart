import 'package:flutter/material.dart';

class DisconnectScreen extends StatelessWidget {
  const DisconnectScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Disconnect Screen"),
      ),
    );
  }
}
