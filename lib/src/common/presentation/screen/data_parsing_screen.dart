import 'package:flutter/material.dart';

class DataParsingScreen extends StatelessWidget {
  const DataParsingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Data Parsing Screen"),
      ),
    );
  }
}
